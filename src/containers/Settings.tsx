import React from 'react';
import Panel from '../components/Panel';

const Settings = (): JSX.Element => {
  return (
    <div className={'content'}>
      <h1>Settings</h1>
      <Panel>Change the settings</Panel>
    </div>
  );
};

export default Settings;
